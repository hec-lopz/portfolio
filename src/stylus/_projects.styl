.projects
  background-color $color-primary
  color $color-white

.grid-container
  display grid
  // max-width 360px
  gap 20px
  grid-template-columns repeat(auto-fill, minmax(calc(320px - 40px), 1fr))
  // margin 0 auto